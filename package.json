{
  "name": "resume",
  "version": "1.0.0",
  "description": "",
  "homepage": "https://resume.jessekelly.me",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:html": "npx resume-cli export --resume resume.yml dist/index.html --theme ./theme",
    "build:pdf": "npx resume-cli export --resume resume.yml dist/resume.pdf --theme ./theme"
  },
  "keywords": [],
  "author": "Jesse Kelly",
  "license": "ISC",
  "dependencies": {
    "chromium": "^3.0.3",
    "puppeteer": "^20.1.1",
    "puppeteer-cli": "^1.5.1",
    "resume-cli": "^3.0.8",
    "resumed": "^3.0.1"
  }
}
